<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Coupon Management</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .coupon-badge {
            position: relative;
            overflow: hidden;
        }
        .coupon-badge::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 50%, rgba(255,255,255,0.1) 100%);
        }
        .progress-bar {
            transition: width 0.6s ease;
        }
        .fade-in {
            animation: fadeIn 0.3s ease-in-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .rotate-chevron {
            transition: transform 0.3s ease;
        }
        .rotate-chevron.down {
            transform: rotate(180deg);
        }
        /* Mobile responsiveness improvements */
        @media (max-width: 768px) {
            /* New mobile list styles */
            .mobile-list-item {
                background-color: rgba(31, 41, 55, 0.5);
                border-bottom: 1px solid rgba(75, 85, 99, 0.5);
                padding: 0.75rem 1rem;
                transition: background-color 0.2s;
            }
            .mobile-list-item:last-child {
                border-bottom: none;
            }
            .mobile-list-item:active {
                background-color: rgba(31, 41, 55, 0.8);
            }
            .mobile-list-main {
                display: flex;
                align-items: center;
                gap: 0.75rem;
            }
            .mobile-list-info {
                flex: 1;
                min-width: 0;
            }
            .mobile-list-name {
                font-size: 0.875rem;
                font-weight: 500;
                color: #ffffff;
                overflow: hidden;
                text-overflow: ellipsis;
                white-space: nowrap;
            }
            .mobile-list-meta {
                display: flex;
                align-items: center;
                gap: 0.5rem;
                margin-top: 0.25rem;
                flex-wrap: wrap;
            }
            .mobile-list-discount {
                font-size: 0.75rem;
                font-weight: 500;
                padding: 0.25rem 0.5rem;
                border-radius: 9999px;
            }
            .mobile-list-status {
                font-size: 0.6875rem; /* 11px */
                padding: 0.25rem 0.5rem;
                border-radius: 9999px;
            }
            /* Expandable details */
            .mobile-details-toggle {
                display: none;
            }
            .mobile-details-content {
                display: none;
                padding: 0.75rem 0;
                border-top: 1px solid rgba(75, 85, 99, 0.3);
                margin-top: 0.5rem;
            }
            .mobile-details-toggle:checked ~ .mobile-details-content {
                display: block;
            }
            .mobile-details-label {
                display: flex;
                justify-content: space-between;
                align-items: center;
                cursor: pointer;
                padding: 0.25rem 0;
                margin-top: 0.5rem;
                border-top: 1px solid rgba(75, 85, 99, 0.3);
            }
            .mobile-details-label i {
                transition: transform 0.2s;
            }
            .mobile-details-toggle:checked ~ .mobile-details-label i {
                transform: rotate(180deg);
            }
            .mobile-details-grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 0.5rem 0.75rem;
                margin-top: 0.5rem;
            }
            .mobile-details-item label {
                font-size: 0.6875rem; /* 11px */
                color: rgba(156, 163, 175, 1);
                text-transform: uppercase;
                letter-spacing: 0.025em;
            }
            .mobile-details-item span {
                font-size: 0.75rem;
                font-weight: 500;
                display: block;
                margin-top: 0.125rem;
            }
            /* Actions */
            .mobile-list-actions {
                display: flex;
                justify-content: flex-end;
                gap: 0.5rem;
                padding: 0.5rem 0;
                border-top: 1px solid rgba(75, 85, 99, 0.3);
            }
            .mobile-list-action-btn {
                padding: 0.5rem;
                border-radius: 0.5rem;
                background-color: rgba(31, 41, 55, 0.7);
                min-width: 2.75rem; /* Ensure touch target ≥44px */
                min-height: 2.75rem;
                display: flex;
                justify-content: center;
                align-items: center;
                transition: background-color 0.2s;
            }
            .mobile-list-action-btn:active {
                background-color: rgba(31, 41, 55, 0.9);
            }
            .mobile-list-action-btn i {
                font-size: 0.875rem;
            }
        }
    </style>
</head>

<body class="bg-gray-900 text-white min-h-screen font-sans antialiased">

    <!-- Navbar -->
    <%- include('../../views/partials/admin/navbar') %>

    <div class="flex h-screen pt-16">
        <!-- Sidebar -->
        <%- include('../../views/partials/admin/sidebar') %>

        <!-- Main Content -->
        <div class="flex-1 ml-0 md:ml-64 p-6 w-full min-w-0 transition-all duration-300">
            <div class="bg-gray-800 rounded-xl shadow-sm border border-gray-600 overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-600 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
                    <div>
                        <h1 class="text-2xl font-bold text-white flex items-center gap-2">
                            <i class="fas fa-tag text-green-500"></i>
                            Coupon Management
                        </h1>
                        <p class="text-sm text-gray-400 mt-1">Manage discount coupons for your store</p>
                    </div>
                    <div class="flex items-center gap-3 w-full sm:w-auto">
                        <button id="openAddModal"
                            class="bg-green-500 hover:bg-green-600 text-white font-medium py-2 px-4 rounded-lg transition duration-300 flex items-center gap-2 whitespace-nowrap">
                            <i class="fas fa-plus"></i>
                            Add Coupon
                        </button>
                    </div>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 px-6 py-4 border-b border-gray-600">
                    <div class="bg-gray-800 border border-gray-600 rounded-lg p-4 shadow-xs">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-sm font-medium text-gray-400">Total Coupons</p>
                                <h3 class="text-2xl font-bold text-white mt-1"><%= coupons.length %></h3>
                            </div>
                            <div class="bg-green-500/20 p-3 rounded-full">
                                <i class="fas fa-tags text-green-500 text-xl"></i>
                            </div>
                        </div>
                    </div>
                    <div class="bg-gray-800 border border-gray-600 rounded-lg p-4 shadow-xs">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-sm font-medium text-gray-400">Active Coupons</p>
                                <h3 class="text-2xl font-bold text-white mt-1">
                                    <%= coupons.filter(c => c.isList && new Date(c.expireOn) > new Date() && (c.usageLimit === 0 || c.usageCount < c.usageLimit)).length %>
                                </h3>
                            </div>
                            <div class="bg-green-500/20 p-3 rounded-full">
                                <i class="fas fa-bolt text-green-500 text-xl"></i>
                            </div>
                        </div>
                    </div>
                    <div class="bg-gray-800 border border-gray-600 rounded-lg p-4 shadow-xs">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-sm font-medium text-gray-400">Expired Coupons</p>
                                <h3 class="text-2xl font-bold text-white mt-1">
                                    <%= coupons.filter(c => new Date(c.expireOn) < new Date()).length %>
                                </h3>
                            </div>
                            <div class="bg-red-500/20 p-3 rounded-full">
                                <i class="fas fa-clock text-red-500 text-xl"></i>
                            </div>
                        </div>
                    </div>
                    <div class="bg-gray-800 border border-gray-600 rounded-lg p-4 shadow-xs">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-sm font-medium text-gray-400">Total Usage</p>
                                <h3 class="text-2xl font-bold text-white mt-1">
                                    <%= coupons.reduce((sum, c) => sum + c.usageCount, 0) %>
                                </h3>
                            </div>
                            <div class="bg-purple-500/20 p-3 rounded-full">
                                <i class="fas fa-users text-purple-500 text-xl"></i>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Desktop Table View -->
                <div class="overflow-x-auto hidden md:block">
                    <table class="min-w-full divide-y divide-gray-600">
                        <thead class="bg-gray-700">
                            <tr>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Coupon Code
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Discount
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Min Purchase
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Usage
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Expires
                                </th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Status
                                </th>
                                <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Actions
                                </th>
                            </tr>
                        </thead>
                        <tbody class="bg-gray-800 divide-y divide-gray-600" id="couponsList">
                            <% if (typeof coupons !=='undefined' && coupons.length> 0) { %>
                                <% coupons.forEach(function(coupon) { %>
                                    <tr class="hover:bg-gray-700 transition">
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <div class="flex items-center">
                                                <div class="coupon-badge bg-gradient-to-r from-green-500 to-green-600 text-white font-bold py-1 px-3 rounded-md text-sm">
                                                    <%= coupon.name %>
                                                </div>
                                            </div>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <div class="text-sm font-medium text-white">
                                                <%= coupon.discountType==='percentage' ? coupon.offerPrice + '%' : '₹' + coupon.offerPrice %>
                                                <span class="text-xs text-gray-400 ml-1">
                                                    (<%= coupon.discountType==='percentage' ? 'Percentage' : 'Fixed' %>)
                                                </span>
                                            </div>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <div class="text-sm text-white">₹<%= coupon.minimumPrice %></div>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <div class="flex flex-col gap-1">
                                                <div class="text-sm text-white">
                                                    <% if (coupon.usageLimit> 0) { %>
                                                        <%= coupon.usageCount %> / <%= coupon.usageLimit %>
                                                    <% } else { %>
                                                        <%= coupon.usageCount %> / ∞
                                                    <% } %>
                                                </div>
                                                <% if (coupon.usageLimit> 0) { %>
                                                    <% var usagePercent=Math.min(100, Math.round((coupon.usageCount / coupon.usageLimit) * 100)); %>
                                                    <div class="w-full bg-gray-600 rounded-full h-1.5">
                                                        <div class="bg-green-500 h-1.5 rounded-full progress-bar" 
                                                            style="width: <%= usagePercent %>%"></div>
                                                    </div>
                                                <% } %>
                                            </div>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <div class="text-sm text-white">
                                                <%= new Date(coupon.expireOn).toLocaleDateString() %>
                                                <% var daysLeft = Math.floor((new Date(coupon.expireOn) - new Date()) / (1000 * 60 * 60 * 24)); %>
                                                <div class="text-xs <%= daysLeft < 3 ? 'text-red-500' : daysLeft < 7 ? 'text-yellow-500' : 'text-green-500' %>">
                                                    <% if (daysLeft > 0) { %>
                                                        <%= daysLeft %> days left
                                                    <% } else { %>
                                                        Expired
                                                    <% } %>
                                                </div>
                                            </div>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <% var isExpired=new Date(coupon.expireOn) < new Date(); %>
                                            <% var usageLimitReached=coupon.usageLimit> 0 && coupon.usageCount >= coupon.usageLimit; %>
                                            <% var isActive=coupon.isList && !isExpired && !usageLimitReached; %>
                                            <span class="px-2.5 py-0.5 inline-flex text-xs leading-5 font-semibold rounded-full 
                                                <%= isActive ? 'bg-green-500/20 text-green-500' : 'bg-red-500/20 text-red-500' %>">
                                                <%= isActive ? 'Active' : 'Inactive' %>
                                                <% if (usageLimitReached) { %>
                                                    <i class="fas fa-exclamation-circle ml-1"></i>
                                                <% } %>
                                            </span>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                            <div class="flex justify-end items-center gap-2">
                                                <button onclick="editCoupon('<%= coupon._id %>')" 
                                                    class="text-green-500 hover:text-green-600 p-2 rounded-full hover:bg-green-500/20 transition"
                                                    title="Edit">
                                                    <i class="fas fa-edit"></i>
                                                </button>
                                                <button onclick="toggleCouponStatus('<%= coupon._id %>', <%= coupon.isList %>)"
                                                    class="<%= coupon.isList ? 'text-red-500 hover:text-red-600' : 'text-green-500 hover:text-green-600' %> p-2 rounded-full <%= coupon.isList ? 'hover:bg-red-500/20' : 'hover:bg-green-500/20' %> transition"
                                                    title="<%= coupon.isList ? 'Disable' : 'Enable' %>">
                                                    <% if (coupon.isList) { %>
                                                        <i class="fas fa-toggle-off"></i>
                                                    <% } else { %>
                                                        <i class="fas fa-toggle-on"></i>
                                                    <% } %>
                                                </button>
                                                <button onclick="toggleDetails('<%= coupon._id %>')"
                                                    class="text-gray-400 hover:text-gray-100 p-2 rounded-full hover:bg-gray-700 transition details-toggle"
                                                    data-coupon="<%= coupon._id %>"
                                                    title="Details">
                                                    <i class="fas fa-chevron-down rotate-chevron"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                    <tr id="details-<%= coupon._id %>" class="hidden bg-gray-700 fade-in">
                                        <td colspan="7" class="px-6 py-4">
                                            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                                                <div class="bg-gray-800 p-4 rounded-lg border border-gray-600 shadow-xs">
                                                    <h3 class="text-sm font-semibold text-white mb-3 flex items-center gap-2">
                                                        <i class="fas fa-info-circle text-green-500"></i>
                                                        Coupon Details
                                                    </h3>
                                                    <div class="space-y-3">
                                                        <div>
                                                            <p class="text-xs text-gray-400">Description</p>
                                                            <p class="text-sm text-white mt-1">
                                                                <%= coupon.description || 'No description provided' %>
                                                            </p>
                                                        </div>
                                                        <div>
                                                            <p class="text-xs text-gray-400">Created On</p>
                                                            <p class="text-sm text-white mt-1">
                                                                <%= new Date(coupon.createdOn).toLocaleDateString() %>
                                                            </p>
                                                        </div>
                                                        <div>
                                                            <p class="text-xs text-gray-400">Last Updated</p>
                                                            <p class="text-sm text-white mt-1">
                                                                <%= coupon.updatedAt ? new Date(coupon.updatedAt).toLocaleDateString() : 'Never' %>
                                                            </p>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="bg-gray-800 p-4 rounded-lg border border-gray-600 shadow-xs">
                                                    <h3 class="text-sm font-semibold text-white mb-3 flex items-center gap-2">
                                                        <i class="fas fa-chart-pie text-green-500"></i>
                                                        Usage Statistics
                                                    </h3>
                                                    <div class="space-y-3">
                                                        <div>
                                                            <p class="text-xs text-gray-400">Total Usage</p>
                                                            <p class="text-sm text-white mt-1">
                                                                <%= coupon.usageCount %> times
                                                            </p>
                                                        </div>
                                                        <div>
                                                            <p class="text-xs text-gray-400">Usage Limit</p>
                                                            <p class="text-sm text-white mt-1">
                                                                <%= coupon.usageLimit > 0 ? coupon.usageLimit : 'Unlimited' %>
                                                            </p>
                                                        </div>
                                                        <div>
                                                            <p class="text-xs text-gray-400">Remaining Uses</p>
                                                            <p class="text-sm <%= coupon.usageLimit > 0 && (coupon.usageLimit - coupon.usageCount) < 5 ? 'text-red-500' : 'text-white' %> mt-1">
                                                                <%= coupon.usageLimit > 0 ? (coupon.usageLimit - coupon.usageCount) : 'Unlimited' %>
                                                            </p>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="bg-gray-800 p-4 rounded-lg border border-gray-600 shadow-xs">
                                                    <div class="flex justify-between items-center mb-3">
                                                        <h3 class="text-sm font-semibold text-white flex items-center gap-2">
                                                            <i class="fas fa-users text-green-500"></i>
                                                            Users (<%= coupon.userId ? coupon.userId.length : 0 %>)
                                                        </h3>
                                                        <button onclick="exportUserData('<%= coupon._id %>', '<%= coupon.name %>')"
                                                            class="text-purple-500 hover:text-purple-600 text-sm font-medium flex items-center gap-1">
                                                            <i class="fas fa-download"></i>
                                                            Export
                                                        </button>
                                                    </div>
                                                    <% if (coupon.userId && coupon.userId.length > 0) { %>
                                                        <div class="overflow-y-auto max-h-40 pr-2">
                                                            <div class="space-y-2">
                                                                <% coupon.userId.slice(0, 5).forEach(function(user) { %>
                                                                    <div class="flex items-center gap-3 p-2 hover:bg-gray-700 rounded">
                                                                        <div class="flex-shrink-0 h-8 w-8 rounded-full bg-green-500/20 flex items-center justify-center text-green-500">
                                                                            <%= user.firstname.charAt(0).toUpperCase() %><%= user.lastname ? user.lastname.charAt(0).toUpperCase() : '' %>
                                                                        </div>
                                                                        <div class="min-w-0">
                                                                            <p class="text-sm font-medium text-white truncate">
                                                                                <%= user.firstname %> <%= user.lastname || '' %>
                                                                            </p>
                                                                            <p class="text-xs text-gray-400 truncate">
                                                                                <%= user.email %>
                                                                            </p>
                                                                        </div>
                                                                    </div>
                                                                <% }); %>
                                                                <% if (coupon.userId.length > 5) { %>
                                                                    <div class="text-center text-xs text-gray-400 mt-2">
                                                                        +<%= coupon.userId.length - 5 %> more users
                                                                    </div>
                                                                <% } %>
                                                            </div>
                                                        </div>
                                                    <% } else { %>
                                                        <div class="text-center py-4">
                                                            <i class="fas fa-user-slash text-gray-400 text-2xl mb-2"></i>
                                                            <p class="text-sm text-gray-400">No users have used this coupon yet</p>
                                                        </div>
                                                    <% } %>
                                                </div>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="7" class="px-6 py-8 text-center">
                                        <div class="flex flex-col items-center justify-center">
                                            <i class="fas fa-tag text-gray-400 text-4xl mb-4"></i>
                                            <h3 class="text-lg font-medium text-white">No coupons found</h3>
                                            <p class="text-gray-400 mt-1">Add your first coupon to start offering discounts</p>
                                            <button id="openAddModalEmpty" class="mt-4 bg-green-500 hover:bg-green-600 text-white font-medium py-2 px-4 rounded-lg transition duration-300 flex items-center gap-2">
                                                <i class="fas fa-plus"></i>
                                                Create Coupon
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <!-- New Mobile List View -->
                <div class="md:hidden">
                    <% if (typeof coupons !=='undefined' && coupons.length> 0) { %>
                        <% coupons.forEach(function(coupon) { %>
                            <div class="mobile-list-item">
                                <!-- Main Row -->
                                <div class="mobile-list-main">
                                    <!-- Coupon Info -->
                                    <div class="mobile-list-info">
                                        <div class="mobile-list-name">
                                            <span class="coupon-badge bg-gradient-to-r from-green-500 to-green-600 text-white font-bold py-1 px-3 rounded-md text-sm">
                                                <%= coupon.name %>
                                            </span>
                                        </div>
                                        <div class="mobile-list-meta">
                                            <span class="mobile-list-discount bg-purple-500/20 text-purple-500">
                                                <%= coupon.discountType==='percentage' ? coupon.offerPrice + '%' : '₹' + coupon.offerPrice %>
                                            </span>
                                            <% var isExpired=new Date(coupon.expireOn) < new Date(); %>
                                            <% var usageLimitReached=coupon.usageLimit> 0 && coupon.usageCount >= coupon.usageLimit; %>
                                            <% var isActive=coupon.isList && !isExpired && !usageLimitReached; %>
                                            <span class="mobile-list-status <%= isActive ? 'bg-green-500/20 text-green-500 border border-green-800' : 'bg-red-500/20 text-red-500 border border-red-800' %>">
                                                <%= isActive ? 'Active' : 'Inactive' %>
                                            </span>
                                            <span class="text-xs text-gray-400">
                                                Usage: <%= coupon.usageCount %> / <%= coupon.usageLimit > 0 ? coupon.usageLimit : '∞' %>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <!-- Expandable Details -->
                                <input type="checkbox" id="mobile-details-<%= coupon._id %>" class="mobile-details-toggle hidden">
                                <label for="mobile-details-<%= coupon._id %>" class="mobile-details-label">
                                    <span class="text-gray-400 text-xs">More Details</span>
                                    <i class="fas fa-chevron-down text-gray-400"></i>
                                </label>
                                <div class="mobile-details-content">
                                    <div class="mobile-details-grid">
                                        <div class="mobile-details-item">
                                            <label>Min Purchase</label>
                                            <span class="text-white">₹<%= coupon.minimumPrice %></span>
                                        </div>
                                        <div class="mobile-details-item">
                                            <label>Expires</label>
                                            <span class="text-white">
                                                <%= new Date(coupon.expireOn).toLocaleDateString() %>
                                                <% var daysLeft = Math.floor((new Date(coupon.expireOn) - new Date()) / (1000 * 60 * 60 * 24)); %>
                                                <div class="text-xs <%= daysLeft < 3 ? 'text-red-500' : daysLeft < 7 ? 'text-yellow-500' : 'text-green-500' %>">
                                                    <% if (daysLeft > 0) { %>
                                                        <%= daysLeft %> days left
                                                    <% } else { %>
                                                        Expired
                                                    <% } %>
                                                </div>
                                            </span>
                                        </div>
                                    </div>
                                    <!-- Existing Details -->
                                    <div class="grid grid-cols-1 gap-6 mt-4">
                                        <div class="bg-gray-800 p-4 rounded-lg border border-gray-600 shadow-xs">
                                            <h3 class="text-sm font-semibold text-white mb-3 flex items-center gap-2">
                                                <i class="fas fa-info-circle text-green-500"></i>
                                                Coupon Details
                                            </h3>
                                            <div class="space-y-3">
                                                <div>
                                                    <p class="text-xs text-gray-400">Description</p>
                                                    <p class="text-sm text-white mt-1">
                                                        <%= coupon.description || 'No description provided' %>
                                                    </p>
                                                </div>
                                                <div>
                                                    <p class="text-xs text-gray-400">Created On</p>
                                                    <p class="text-sm text-white mt-1">
                                                        <%= new Date(coupon.createdOn).toLocaleDateString() %>
                                                    </p>
                                                </div>
                                                <div>
                                                    <p class="text-xs text-gray-400">Last Updated</p>
                                                    <p class="text-sm text-white mt-1">
                                                        <%= coupon.updatedAt ? new Date(coupon.updatedAt).toLocaleDateString() : 'Never' %>
                                                    </p>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="bg-gray-800 p-4 rounded-lg border border-gray-600 shadow-xs">
                                            <h3 class="text-sm font-semibold text-white mb-3 flex items-center gap-2">
                                                <i class="fas fa-chart-pie text-green-500"></i>
                                                Usage Statistics
                                            </h3>
                                            <div class="space-y-3">
                                                <div>
                                                    <p class="text-xs text-gray-400">Total Usage</p>
                                                    <p class="text-sm text-white mt-1">
                                                        <%= coupon.usageCount %> times
                                                    </p>
                                                </div>
                                                <div>
                                                    <p class="text-xs text-gray-400">Usage Limit</p>
                                                    <p class="text-sm text-white mt-1">
                                                        <%= coupon.usageLimit > 0 ? coupon.usageLimit : 'Unlimited' %>
                                                    </p>
                                                </div>
                                                <div>
                                                    <p class="text-xs text-gray-400">Remaining Uses</p>
                                                    <p class="text-sm <%= coupon.usageLimit > 0 && (coupon.usageLimit - coupon.usageCount) < 5 ? 'text-red-500' : 'text-white' %> mt-1">
                                                        <%= coupon.usageLimit > 0 ? (coupon.usageLimit - coupon.usageCount) : 'Unlimited' %>
                                                    </p>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="bg-gray-800 p-4 rounded-lg border border-gray-600 shadow-xs">
                                            <div class="flex justify-between items-center mb-3">
                                                <h3 class="text-sm font-semibold text-white flex items-center gap-2">
                                                    <i class="fas fa-users text-green-500"></i>
                                                    Users (<%= coupon.userId ? coupon.userId.length : 0 %>)
                                                </h3>
                                            </div>
                                            <% if (coupon.userId && coupon.userId.length > 0) { %>
                                                <div class="overflow-y-auto max-h-40 pr-2">
                                                    <div class="space-y-2">
                                                        <% coupon.userId.slice(0, 5).forEach(function(user) { %>
                                                            <div class="flex items-center gap-3 p-2 hover:bg-gray-700 rounded">
                                                                <div class="flex-shrink-0 h-8 w-8 rounded-full bg-green-500/20 flex items-center justify-center text-green-500">
                                                                    <%= user.firstname.charAt(0).toUpperCase() %><%= user.lastname ? user.lastname.charAt(0).toUpperCase() : '' %>
                                                                </div>
                                                                <div class="min-w-0">
                                                                    <p class="text-sm font-medium text-white truncate">
                                                                        <%= user.firstname %> <%= user.lastname || '' %>
                                                                    </p>
                                                                    <p class="text-xs text-gray-400 truncate">
                                                                        <%= user.email %>
                                                                    </p>
                                                                </div>
                                                            </div>
                                                        <% }); %>
                                                        <% if (coupon.userId.length > 5) { %>
                                                            <div class="text-center text-xs text-gray-400 mt-2">
                                                                +<%= coupon.userId.length - 5 %> more users
                                                            </div>
                                                        <% } %>
                                                    </div>
                                                </div>
                                            <% } else { %>
                                                <div class="text-center py-4">
                                                    <i class="fas fa-user-slash text-gray-400 text-2xl mb-2"></i>
                                                    <p class="text-sm text-gray-400">No users have used this coupon yet</p>
                                                </div>
                                            <% } %>
                                        </div>
                                    </div>
                                </div>
                                <!-- Actions -->
                                <div class="mobile-list-actions">
                                    <button onclick="editCoupon('<%= coupon._id %>')" 
                                            class="mobile-list-action-btn text-green-500 hover:text-green-600" 
                                            title="Edit">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                    <button onclick="toggleCouponStatus('<%= coupon._id %>', <%= coupon.isList %>)" 
                                            class="mobile-list-action-btn <%= coupon.isList ? 'text-red-500 hover:text-red-600' : 'text-green-500 hover:text-green-600' %>" 
                                            title="<%= coupon.isList ? 'Disable' : 'Enable' %>">
                                        <i class="fas <%= coupon.isList ? 'fa-toggle-off' : 'fa-toggle-on' %>"></i>
                                    </button>
                                    <button onclick="toggleDetails('<%= coupon._id %>')" 
                                            class="mobile-list-action-btn text-gray-400 hover:text-gray-100 details-toggle" 
                                            data-coupon="<%= coupon._id %>" 
                                            title="Details">
                                        <i class="fas fa-chevron-down rotate-chevron"></i>
                                    </button>
                                    <button onclick="exportUserData('<%= coupon._id %>', '<%= coupon.name %>')" 
                                            class="mobile-list-action-btn text-purple-500 hover:text-purple-600" 
                                            title="Export Users">
                                        <i class="fas fa-download"></i>
                                    </button>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="px-6 py-8 text-center">
                            <div class="flex flex-col items-center justify-center">
                                <i class="fas fa-tag text-gray-400 text-4xl mb-4"></i>
                                <h3 class="text-lg font-medium text-white">No coupons found</h3>
                                <p class="text-gray-400 mt-1">Add your first coupon to start offering discounts</p>
                                <button id="openAddModalEmpty" class="mt-4 bg-green-500 hover:bg-green-600 text-white font-medium py-2 px-4 rounded-lg transition duration-300 flex items-center gap-2">
                                    <i class="fas fa-plus"></i>
                                    Create Coupon
                                </button>
                            </div>
                        </div>
                    <% } %>
                </div>

                <!-- Pagination -->
                <% if (typeof coupons !=='undefined' && coupons.length > 0) { %>
                    <div class="px-6 py-4 border-t border-gray-600 flex flex-col sm:flex-row items-center justify-between gap-4">
                        <div class="text-sm text-gray-400 text-center sm:text-left">
                            <% const start = (currentPage - 1) * 5 + 1 %>
                            <% const end = Math.min(start + coupons.length - 1, totalCoupons) %>
                            Showing <span class="font-medium"><%= start %></span> to <span class="font-medium"><%= end %></span> of <span class="font-medium"><%= totalCoupons %></span> coupons
                        </div>
                        <div class="flex flex-wrap justify-center gap-2">
                            <% if (currentPage > 1) { %>
                                <a href="/admin/coupons?page=<%= currentPage - 1 %>" class="px-3 py-1 border border-gray-600 rounded-md text-sm font-medium text-white bg-gray-700 hover:bg-gray-600 transition-colors">
                                    <i class="fas fa-chevron-left mr-1"></i> Previous
                                </a>
                            <% } else { %>
                                <button disabled class="px-3 py-1 border border-gray-700 rounded-md text-sm font-medium text-gray-500 bg-gray-800 cursor-not-allowed">
                                    <i class="fas fa-chevron-left mr-1"></i> Previous
                                </button>
                            <% } %>
                            
                            <% for (let i = 1; i <= totalPages; i++) { %>
                                <a href="/admin/coupons?page=<%= i %>" class="px-3 py-1 border <%= currentPage === i ? 'border-green-500' : 'border-gray-600' %> rounded-md text-sm font-medium text-white <%= currentPage === i ? 'bg-green-500 hover:bg-green-600' : 'bg-gray-700 hover:bg-gray-600' %> transition-colors">
                                    <%= i %>
                                </a>
                            <% } %>
                            
                            <% if (currentPage < totalPages) { %>
                                <a href="/admin/coupons?page=<%= currentPage + 1 %>" class="px-3 py-1 border border-gray-600 rounded-md text-sm font-medium text-white bg-gray-700 hover:bg-gray-600 transition-colors">
                                    Next <i class="fas fa-chevron-right ml-1"></i>
                                </a>
                            <% } else { %>
                                <button disabled class="px-3 py-1 border border-gray-700 rounded-md text-sm font-medium text-gray-500 bg-gray-800 cursor-not-allowed">
                                    Next <i class="fas fa-chevron-right ml-1"></i>
                                </button>
                            <% } %>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <div id="addCouponModal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
        <div class="relative bg-white rounded-xl shadow-xl max-w-2xl w-full mx-4 overflow-hidden fade-in">
            <div class="flex justify-between items-center p-5 border-b border-gray-600">
                <div>
                    <h3 class="text-xl font-semibold text-black" id="modalTitle">Add New Coupon</h3>
                    <p class="text-sm text-gray-400 mt-1" id="modalSubtitle">Create a discount coupon for your customers</p>
                </div>
                <button id="closeAddModal" class="text-gray-400 hover:text-gray-100 p-1 rounded-full hover:bg-gray-700">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>
            <form id="couponForm" action="/admin/add-coupon" method="POST">
                <input type="hidden" id="couponId" name="couponId">
                <div class="p-6 grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="space-y-4">
                        <div>
                            <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Coupon Code</label>
                            <div class="relative">
                                <input type="text" name="name" id="name" 
                                    class="block w-full pl-10 pr-3 py-2 border border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition bg-gray-700 text-white"
                                    required>
                                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                    <i class="fas fa-tag text-gray-400"></i>
                                </div>
                            </div>
                            <p id="nameHelpText" class="mt-1 text-xs text-gray-400">Code should be uppercase without spaces (e.g., SUMMER25)</p>
                        </div>
                        <div>
                            <label for="discountType" class="block text-sm font-medium text-gray-700 mb-1">Discount Type</label>
                            <div class="relative">
                                <select name="discountType" id="discountType" 
                                    class="block w-full pl-10 pr-3 py-2 border border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition appearance-none bg-gray-700 text-white"
                                    required>
                                    <option value="amount">Fixed Amount (₹)</option>
                                    <option value="percentage">Percentage (%)</option>
                                </select>
                                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                    <i class="fas fa-percent text-gray-400"></i>
                                </div>
                                <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                    <i class="fas fa-chevron-down text-gray-400"></i>
                                </div>
                            </div>
                        </div>
                        <div>
                            <label for="minimumPrice" class="block text-sm font-medium text-gray-700 mb-1">Minimum Purchase (₹)</label>
                            <div class="relative">
                                <input type="number" name="minimumPrice" id="minimumPrice" min="0" 
                                    class="block w-full pl-10 pr-3 py-2 border border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition bg-gray-700 text-white"
                                    required>
                                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                    <i class="fas fa-indian-rupee-sign text-gray-400"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="space-y-4">
                        <div>
                            <label for="description" class="block text-sm font-medium text-gray-700 mb-1">Description</label>
                            <div class="relative">
                                <textarea name="description" id="description" rows="2"
                                    class="block w-full pl-10 pr-3 py-2 border border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition bg-gray-700 text-white"></textarea>
                                <div class="absolute top-2 left-3">
                                    <i class="fas fa-align-left text-gray-400"></i>
                                </div>
                            </div>
                            <p class="mt-1 text-xs text-gray-400">Optional: Add a brief description about this coupon</p>
                        </div>
                        <div>
                            <label for="offerPrice" class="block text-sm font-medium text-gray-700 mb-1">Discount Value</label>
                            <div class="relative">
                                <input type="number" name="offerPrice" id="offerPrice" min="1" 
                                    class="block w-full pl-10 pr-3 py-2 border border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition bg-gray-700 text-white"
                                    required>
                                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                    <i id="discountIcon" class="fas fa-indian-rupee-sign text-gray-400"></i>
                                </div>
                            </div>
                            <p class="mt-1 text-xs text-gray-400" id="offerPriceNote">For fixed amount: must be less than minimum purchase. For percentage: must be between 1-100.</p>
                        </div>
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <label for="usageLimit" class="block text-sm font-medium text-gray-700 mb-1">Usage Limit</label>
                                <div class="relative">
                                    <input type="number" name="usageLimit" id="usageLimit" min="0" 
                                        class="block w-full pl-10 pr-3 py-2 border border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition bg-gray-700 text-white">
                                    <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                        <i class="fas fa-users text-gray-400"></i>
                                    </div>
                                </div>
                                <p class="mt-1 text-xs text-gray-400">0 = unlimited uses</p>
                            </div>
                            <div>
                                <label for="expireOn" class="block text-sm font-medium text-gray-700 mb-1">Expiry Date</label>
                                <div class="relative">
                                    <input type="date" name="expireOn" id="expireOn" 
                                        class="block w-full pl-10 pr-3 py-2 border border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition bg-gray-700 text-white"
                                        required>
                                    <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                        <i class="fas fa-calendar-day text-gray-400"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="px-6 py-4 bg-gray-700 rounded-b-xl flex justify-end gap-3">
                    <button type="button" id="cancelButton"
                        class="bg-gray-700 hover:bg-gray-600 text-white font-medium py-2 px-4 border border-gray-600 rounded-lg transition">
                        Cancel
                    </button>
                    <button type="submit" id="submitButton"
                        class="bg-green-500 hover:bg-green-600 text-white font-medium py-2 px-4 rounded-lg transition flex items-center gap-2">
                        <i class="fas fa-save"></i>
                        <span id="submitButtonText">Save Coupon</span>
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script>
        var addModal = document.getElementById('addCouponModal');
        var openAddModalBtn = document.getElementById('openAddModal');
        var openAddModalEmptyBtn = document.getElementById('openAddModalEmpty');
        var closeAddModalBtn = document.getElementById('closeAddModal');
        var cancelButton = document.getElementById('cancelButton');
        var couponForm = document.getElementById('couponForm');
        var modalTitle = document.getElementById('modalTitle');
        var modalSubtitle = document.getElementById('modalSubtitle');
        var submitButtonText = document.getElementById('submitButtonText');
        var discountIcon = document.getElementById('discountIcon');

        var expireOnInput = document.getElementById('expireOn');
        var offerPriceInput = document.getElementById('offerPrice');
        var minimumPriceInput = document.getElementById('minimumPrice');
        var discountTypeInput = document.getElementById('discountType');
        var nameInput = document.getElementById('name');
        var nameHelpText = document.getElementById('nameHelpText');

        var today = new Date();
        var formattedDate = today.toISOString().split('T')[0];
        expireOnInput.min = formattedDate;

        function updateDiscountIcon() {
            if (discountTypeInput.value === 'percentage') {
                discountIcon.className = 'fas fa-percent text-gray-400';
            } else {
                discountIcon.className = 'fas fa-indian-rupee-sign text-gray-400';
            }
        }

        function updateOfferPriceLabel() {
            var discountType = discountTypeInput.value;
            var offerPriceNote = document.getElementById('offerPriceNote');

            if (discountType === 'percentage') {
                offerPriceNote.textContent = 'Percentage discount must be between 1 and 100';
                offerPriceInput.max = 100;
            } else {
                offerPriceNote.textContent = 'Discount amount must be less than minimum purchase amount';
                offerPriceInput.removeAttribute('max');
            }
            updateDiscountIcon();
        }

        var validatePrices = function () {
            var offerPrice = Number(offerPriceInput.value);
            var minimumPrice = Number(minimumPriceInput.value);
            var discountType = discountTypeInput.value;

            if (discountType === 'percentage') {
                if (offerPrice <= 0 || offerPrice > 100) {
                    offerPriceInput.setCustomValidity('Percentage discount must be between 1 and 100');
                    return false;
                } else {
                    offerPriceInput.setCustomValidity('');
                    return true;
                }
            } else {
                if (offerPrice && minimumPrice && offerPrice >= minimumPrice) {
                    offerPriceInput.setCustomValidity('Discount amount must be less than minimum purchase amount');
                    return false;
                } else {
                    offerPriceInput.setCustomValidity('');
                    return true;
                }
            }
        };

        offerPriceInput.addEventListener('input', validatePrices);
        minimumPriceInput.addEventListener('input', validatePrices);
        discountTypeInput.addEventListener('change', function () {
            validatePrices();
            updateOfferPriceLabel();
        });

        function initModal() {
            updateOfferPriceLabel();
        }

        function openAddModal() {
            modalTitle.textContent = 'Add New Coupon';
            modalSubtitle.textContent = 'Create a discount coupon for your customers';
            submitButtonText.textContent = 'Save Coupon';
            couponForm.reset();
            couponForm.action = '/admin/add-coupon';
            document.getElementById('couponId').value = '';
            nameInput.readOnly = false;
            nameHelpText.textContent = 'Code should be uppercase without spaces (e.g., SUMMER25)';
            nameHelpText.classList.remove('hidden');
            addModal.classList.remove('hidden');
            initModal();
        }

        if (openAddModalEmptyBtn) {
            openAddModalEmptyBtn.addEventListener('click', openAddModal);
        }

        openAddModalBtn.addEventListener('click', openAddModal);

        function closeModal() {
            addModal.classList.add('hidden');
        }

        closeAddModalBtn.addEventListener('click', closeModal);
        cancelButton.addEventListener('click', closeModal);

        addModal.addEventListener('click', function (e) {
            if (e.target === addModal) {
                closeModal();
            }
        });

        function editCoupon(couponId) {
            modalTitle.textContent = 'Edit Coupon';
            modalSubtitle.textContent = 'Update coupon details';
            submitButtonText.textContent = 'Update Coupon';
            couponForm.action = '/admin/edit-coupon';
            document.getElementById('couponId').value = couponId;

            fetch('/admin/get-coupon/' + couponId)
                .then(function (response) { return response.json(); })
                .then(function (coupon) {
                    nameHelpText.textContent = 'Coupon name cannot be changed after creation';
                    nameHelpText.classList.remove('hidden');
                    nameInput.value = coupon.name;
                    nameInput.readOnly = true;
                    document.getElementById('description').value = coupon.description || '';
                    document.getElementById('offerPrice').value = coupon.offerPrice;
                    document.getElementById('minimumPrice').value = coupon.minimumPrice;
                    document.getElementById('discountType').value = coupon.discountType || 'amount';
                    document.getElementById('usageLimit').value = coupon.usageLimit || 0;
                    document.getElementById('expireOn').value = new Date(coupon.expireOn).toISOString().split('T')[0];
                    updateOfferPriceLabel();
                    addModal.classList.remove('hidden');
                    initModal();
                })
                .catch(function (error) {
                    console.error('Error fetching coupon details:', error);
                    swal("Error", "Failed to fetch coupon details", "error");
                });
        }

        // Toast notification mixin
        const Toast = Swal.mixin({
            toast: true,
            position: 'top-end',
            showConfirmButton: false,
            timer: 3000,
            timerProgressBar: true,
            didOpen: (toast) => {
                toast.addEventListener('mouseenter', Swal.stopTimer)
                toast.addEventListener('mouseleave', Swal.resumeTimer)
            }
        });
        
        function toggleCouponStatus(couponId, currentStatus) {
            var action = currentStatus ? 'disable' : 'enable';
            var actionTitle = action.charAt(0).toUpperCase() + action.slice(1);

            Swal.fire({
                title: actionTitle + " this coupon?",
                text: currentStatus ? "Users won't be able to use this coupon anymore." : "This will make the coupon available to users.",
                icon: "warning",
                showCancelButton: true,
                confirmButtonText: actionTitle,
                cancelButtonText: "Cancel",
                confirmButtonColor: currentStatus ? '#ef4444' : '#22c55e',
                cancelButtonColor: '#374151',
                reverseButtons: true
            }).then((result) => {
                if (result.isConfirmed) {
                    // Show loading toast
                    Toast.fire({
                        icon: 'info',
                        title: 'Processing...',
                        timer: 1000,
                        showConfirmButton: false
                    });
                    
                    fetch('/admin/toggle-coupon/' + couponId, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ status: !currentStatus })
                    })
                    .then(function (response) { return response.json(); })
                    .then(function (data) {
                        if (data.success) {
                            // Show success toast
                            Toast.fire({
                                icon: 'success',
                                title: "Coupon " + action + "d successfully"
                            });
                            
                            // Reload the page after a short delay
                            setTimeout(() => {
                                window.location.reload();
                            }, 1000);
                        } else {
                            // Show error toast
                            Toast.fire({
                                icon: 'error',
                                title: data.message || "Something went wrong"
                            });
                        }
                    })
                    .catch(function (error) {
                        console.error('Error toggling coupon status:', error);
                        
                        // Show error toast
                        Toast.fire({
                            icon: 'error',
                            title: "Failed to update coupon status"
                        });
                    });
                }
            });
        }

        couponForm.addEventListener('submit', function (event) {
            event.preventDefault();

            var couponName = nameInput.value;
            if (couponName !== couponName.toUpperCase() || couponName.includes(' ')) {
                const Toast = Swal.mixin({
                    toast: true,
                    position: 'top-end',
                    showConfirmButton: false,
                    timer: 3000,
                    timerProgressBar: true,
                    didOpen: (toast) => {
                        toast.addEventListener('mouseenter', Swal.stopTimer)
                        toast.addEventListener('mouseleave', Swal.resumeTimer)
                    }
                });
                
                Toast.fire({
                    icon: 'warning',
                    title: 'Coupon code must be uppercase and contain no spaces'
                });
                return;
            }

            var offerPrice = Number(offerPriceInput.value);
            var minimumPrice = Number(minimumPriceInput.value);
            var discountType = discountTypeInput.value;

            if (discountType === 'percentage') {
                if (offerPrice <= 0 || offerPrice > 100) {
                    Toast.fire({
                        icon: 'warning',
                        title: 'Percentage discount must be between 1 and 100'
                    });
                    return;
                }
            } else {
                if (offerPrice >= minimumPrice) {
                    Toast.fire({
                        icon: 'warning',
                        title: 'Discount amount must be less than minimum purchase amount'
                    });
                    return;
                }
            }

            var formAction = couponForm.action;
            var isEdit = formAction.includes('edit-coupon');

            var formData = {
                name: couponName,
                description: document.getElementById('description').value,
                offerPrice: offerPrice,
                minimumPrice: minimumPrice,
                discountType: discountType,
                usageLimit: Number(document.getElementById('usageLimit').value) || 0,
                expireOn: document.getElementById('expireOn').value
            };

            if (isEdit) {
                formData.couponId = document.getElementById('couponId').value;
            }

            var submitButton = document.getElementById('submitButton');
            var originalButtonHTML = submitButton.innerHTML;
            submitButton.innerHTML = '<i class="fas fa-spinner fa-spin"></i> ' + (isEdit ? 'Updating...' : 'Creating...');
            submitButton.disabled = true;

            fetch(formAction, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(formData)
            })
                .then(function (response) { return response.json(); })
                .then(function (data) {
                    submitButton.innerHTML = originalButtonHTML;
                    submitButton.disabled = false;
                    
                    // Close the modal first
                    closeModal();
                    
                    if (data.success) {
                        Toast.fire({
                            icon: 'success',
                            title: isEdit ? 'Coupon updated successfully' : 'Coupon created successfully'
                        }).then(function () {
                            window.location.reload();
                        });
                    } else {
                        Toast.fire({
                            icon: 'error',
                            title: data.message || 'Something went wrong'
                        });
                    }
                })
                .catch(function (error) {
                    submitButton.innerHTML = originalButtonHTML;
                    submitButton.disabled = false;
                    console.error('Error submitting coupon form:', error);
                    
                    // Close the modal even on error
                    closeModal();
                    
                    Toast.fire({
                        icon: 'error',
                        title: 'Failed to ' + (isEdit ? 'update' : 'create') + ' coupon'
                    });
                });
        });

        function toggleDetails(couponId) {
            var detailsRow = document.getElementById('details-' + couponId);
            var mobileDetailsToggle = document.getElementById('mobile-details-' + couponId);
            var toggleButton = document.querySelector(`.details-toggle[data-coupon="${couponId}"] i`);
            
            if (detailsRow) {
                detailsRow.classList.toggle('hidden');
            }
            if (mobileDetailsToggle) {
                mobileDetailsToggle.checked = !mobileDetailsToggle.checked;
            }
            if (toggleButton) {
                toggleButton.classList.toggle('down');
            }
        }

        function exportUserData(couponId, couponName) {
            swal({
                title: "Export User Data?",
                text: "This will download a CSV file with all users who used this coupon.",
                icon: "info",
                buttons: {
                    cancel: {
                        text: "Cancel",
                        value: null,
                        visible: true,
                        className: "bg-gray-700 hover:bg-gray-600 text-white font-medium py-2 px-4 rounded-lg",
                        closeModal: true,
                    },
                    confirm: {
                        text: "Export",
                        value: true,
                        visible: true,
                        className: "bg-green-500 hover:bg-green-600 text-white font-medium py-2 px-4 rounded-lg",
                        closeModal: true
                    }
                }
            }).then((confirm) => {
                if (confirm) {
                    window.location.href = `/admin/export-coupon-users/${couponId}?name=${encodeURIComponent(couponName)}`;
                }
            });
        }

        document.addEventListener('DOMContentLoaded', function() {
            var today = new Date();
            var nextWeek = new Date();
            nextWeek.setDate(today.getDate() + 7);
            document.getElementById('expireOn').valueAsDate = nextWeek;
        });
    </script>
</body>
</html>